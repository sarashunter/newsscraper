<div class="jumbotron">
    <h1><a href="{{article.url}}">{{article.title}}</a></h1>
    <p class="lead">By: {{article.author}}</p>

    <div class="input-group mb-3 input-group-lg">
        <input type="text" class="form-control" id="comment" placeholder="Comment">
        <div class="input-group-append">
            <button class="btn btn-outline-secondary" type="submit" id="submit" data-id="{{article.id}}">Submit</button>
        </div>
    </div>


    <div id="comments">
        {{#each article.comments}}
        {{#if this.exists}}
        <div id={{this._id}}>
            <button class="delete" data-id="{{this._id}}" data-commentId="{{this._id}}"><img src="/assets/open-iconic-master/svg/trash.svg" alt="icon name"></button>
            {{this.body}}
        </div>
        {{/if}}
        {{/each}}

    </div>
</div>